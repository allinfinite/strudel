{
  "buildCommand": "pnpm --filter website build && mkdir -p .vercel/output/static && cp -r website/dist/. .vercel/output/static && echo '{\"version\":3}' > .vercel/output/config.json",
  "installCommand": "pnpm install && pnpm -w run jsdoc-json",
  "framework": null
}
