{
  "buildCommand": "pnpm --filter website run build && if [ -d website ]; then cd website; fi; mkdir -p ../.vercel/output/static && cp -R dist/. ../.vercel/output/static && mkdir -p ../.vercel/output && echo '{\"version\":3}' > ../.vercel/output/config.json",
  "installCommand": "pnpm install && pnpm -w run jsdoc-json",
  "framework": null
}
