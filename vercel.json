{
  "buildCommand": "pnpm --filter website run build && SRC=dist; if [ ! -d \"$SRC\" ]; then SRC=website/dist; fi; mkdir -p .vercel/output/static && cp -R \"$SRC\"/* .vercel/output/static && mkdir -p .vercel/output && echo '{\"version\":3}' > .vercel/output/config.json",
  "installCommand": "pnpm install && pnpm -w run jsdoc-json",
  "framework": null
}
