{
  "buildCommand": "pnpm --filter website build && R=$(pwd) && mkdir -p $R/.vercel/output/static && cp -r $R/website/dist/. $R/.vercel/output/static && echo '{\"version\":3}' > $R/.vercel/output/config.json",
  "installCommand": "pnpm install && pnpm -w run jsdoc-json",
  "framework": null
}
